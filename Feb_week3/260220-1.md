# 🔤 문자열과 NUL 문자(`\0`), 그리고 포인터 연산

## 1. 문자열과 NUL 문자의 관계
* 문자열의 끝에는 반드시 **`\0` (NUL, NULL 문자)**가 있어야 합니다.
* 문자열을 배열에 저장할 때 `strlen()` 함수는 NUL 문자를 포함하지 않고 길이를 구하지만, `sizeof` 연산자는 NUL 문자를 포함한 전체 메모리 크기를 구합니다.
* **NUL 문자의 특징:** 16진수로 **`0x00`** 값을 가지며, GDB 메모리 검사 시 `00`으로 확인됩니다.

### 실습: sizeof와 strlen의 차이 (`array8.c`)
```c
// array8.c
#include <stdio.h>
#include <string.h>  // strlen

int main()
{
    char message[] = "Hello, C!";          // 문자 9개 + \0 = 10byte
    char message2[] = "ABCD\0EFGHIJKLMN"; // 중간에 \0 삽입

    printf("sizeof(message) = %ld\n", sizeof(message)); // 10
    printf("strlen(message) = %ld\n", strlen(message)); // 9

    printf("message2: %s\n", message2);                 // ABCD (NUL에서 출력 멈춤)
    printf("sizeof(message2) = %ld\n", sizeof(message2)); // 16
    printf("strlen(message2) = %ld\n", strlen(message2)); // 4
    
    return 0;
}
```

---

## 2. NUL 문자가 없는 배열의 위험성 (`array9.c`)

배열에 문자를 하나씩 저장할 때 마지막에 `\0`를 넣지 않으면, `%s`로 출력 시 메모리 상에서 다음 `\0`를 만날 때까지 인접한 메모리(다른 변수 영역)를 계속 출력하게 됩니다.

```c
// array9.c
#include <stdio.h>

int main()
{
    char ch[6]; // 'A'~'F'까지 6개만 할당 (NUL 문자 공간 없음)
    ch[0] = 'A'; ch[1] = 'B'; ch[2] = 'C'; ch[3] = 'D'; ch[4] = 'E'; ch[5] = 'F';

    char ch2[] = "Hello";

    printf("%c%c%c%c%c%c\n", ch[0], ch[1], ch[2], ch[3], ch[4], ch[5]); // %c 출력은 정상
    printf("%s\n", ch); // [위험] ch 끝에 \0가 없어 ch2인 Hello까지 함께 출력됨
    printf("%s\n", ch2);

    return 0;
}
```

### 메모리 배치 도식화
```text
ch                      ch2
+---+---+---+---+---+---+---+---+---+---+---+----+
| A | B | C | D | E | F | H | e | l | l | o | \0 |
+---+---+---+---+---+---+---+---+---+---+---+----+
```
**출력 결과:**
`ABCDEFHello` <-- `ch`의 범위를 넘어 `ch2` 영역까지 읽어버린 결과

---

## 3. 포인터를 이용한 문자열 탐색 (`array10.c`, `array11.c`)

포인터는 **`+`, `-` 연산이 가능**하여 주소를 앞뒤로 이동할 수 있지만, `*`, `/` 연산은 불가능합니다.

### 포인터 증감 실습 (`array10.c`)
```c
#include <stdio.h>

int main()
{
    int a = 1;
    char ch[5] = "ABCD";
    char *cp = ch; // cp = &ch[0];

    printf("ch: %s, *cp: %s\n", ch, cp); // ABCD ABCD

    while(*cp != '\0') // 포인터가 가리키는 값이 NUL이 아닐 때까지 반복
    {
        printf("%c\n", *cp);
        cp++; // 포인터 주소를 1씩 증가시켜 다음 문자로 이동
    }
    return 0;
}
```

---

## 🐞 GDB 정밀 분석 및 메모리 도식 (`array11.c`)

GDB를 통해 포인터 `cp`가 주소를 이동하며 문자열을 어떻게 한 글자씩 읽는지 분석합니다.



### GDB 분석 과정
1. **초기 주소 확인:** `cp`에 `ch`의 시작 주소 `0x...b3`이 저장됨.
2. **값 검사:** `*cp`를 확인하면 첫 글자인 `'A'`(65)가 나타남.
3. **주소 이동:** `cp++` 실행 후 주소가 `0x...b4`로 증가하며 다음 문자인 `'B'`를 가리킴.
4. **종료:** `cp`가 `0x...b7`에 도달하면 `*cp`가 `\0`이 되어 루프를 종료함.

### 메모리 및 포인터 관계도
```text
cp                ch (0x7fffffffe1b3 ~ )
+------------+    +---+---+---+---+----+
| 0x...b3    |--->| A | B | C | D | \0 |
+------------+    +---+---+---+---+----+
                  |   |   |   |   |
                  |   |   |   |   +-- 0x...b7 (\0)
                  |   |   |   +-- 0x...b6 (D)
                  |   |   +-- 0x...b5 (C)
                  |   +-- 0x...b4 (B)
                  +-- 0x...b3 (A)
```

### GDB 주요 로그
```gdb
(gdb) p &ch[0]
$2 = 0x7fffffffe1b3 "ABCD"
(gdb) p/x cp
$6 = 0x7fffffffe1b3
(gdb) p *cp
$5 = 65 'A'
(gdb) n  # cp++ 실행 후
(gdb) p/x cp
$8 = 0x7fffffffe1b4  # 주소 1 증가 확인
(gdb) p *cp
$9 = 66 'B'
(gdb) x/xb 0x7fffffffe1b7
0x7fffffffe1b7: 0x00  # NUL 문자 확인
```
