# 🔄 C언어 for문 완벽 정리 (기초부터 이중 for문 실습까지)

## 1. for문의 기초

### 1) 형식 및 동작 순서
`for`문은 초기식, 조건식, 증감식을 한 줄에 모아놓은 반복문입니다.

```c
int i;

// 순서: ①초깃값 -> ②조건식 -> ③실행문 -> ④증감식 -> ②조건식 ...
for(i = 1; i <= 5; i++)
{
    printf("%d\n", i);  // 실행문
}
```

### 2) 기본 예제 (`for1.c`)
변수 선언 위치에 따른 유효 범위(Scope)를 확인합니다.

```c
// for1.c
#include <stdio.h>

int main()
{
    // C99 표준부터 for문 초기식 내에서 변수 선언 가능
    // 단, 여기서 선언된 i는 for문 블록 {} 안에서만 유효함
    for(int i = 1; i <= 5; i++)
    {
        printf("%d\n", i);
    }

    // printf("%d\n", i);  // [에러] i는 for문이 끝나서 소멸됨
    return 0;
}
```

---

## 2. 변수의 유효 범위 (Scope) 증명

중괄호 `{ }` 블록 내에서 선언된 변수는 해당 블록이 끝나면 사라집니다.

```c
// scope_test.c
#include <stdio.h>

int main() {
    int c = 3;  // main 함수 전체에서 유효

    {
        int a = 1; // 이 블록 안에서만 유효
        printf("a = %d\n", a);

        {
            int b = 2; // 가장 안쪽 블록에서만 유효
            // 상위 블록의 a, c 접근 가능
            printf("a = %d, b = %d, c = %d\n", a, b, c);
        }
        // printf("b = %d\n", b); // [에러] b는 소멸됨
        printf("c = %d\n", c);    // c는 접근 가능
    }
    // printf("a = %d\n", a); // [에러] a는 소멸됨
    
    return 0;
}
```

---

## 3. 구구단 출력하기 (기초)

### 1) 2단 출력 (`for2.c`)
```c
// for2.c
#include <stdio.h>

int main() {
    int dan = 2;
    int i;

    // 1부터 9까지 반복
    for(i = 1; i <= 9; i++)
        printf("%dx%d=%d  ", dan, i, dan*i);

    putchar('\n');  // 줄바꿈
    return 0;
}
```

### 2) 입력받은 단 출력 (`for3.c`)
```c
// for3.c
#include <stdio.h>

int main() {
    int dan, i;

    printf("구구단 몇 단을 출력하겠습니까? (2 ~ 9) ");
    scanf("%d", &dan);

    for(i = 1; i <= 9; i++)
        printf("%dx%d=%d  ", dan, i, dan*i);

    putchar('\n');
    return 0;
}
```

---

## 4. for문의 무한 루프 (`for4.c`)
조건식을 비워두면(`;;`) 무한 루프가 됩니다. 내부에서 `break`로 탈출해야 합니다.

```c
// for4.c
#include <stdio.h>

int main() {
    int i = 1;

    for(;;) { // 무한 루프
        printf("%d\n", i); // 실행문
        
        i++; // 증감
        
        // 탈출 조건
        if(i == 6) break;
    }
    return 0;
}
```

---

## 5. 이중 for문 (Nested Loop)

### 1) 구조
반복문 안에 또 다른 반복문이 들어있는 형태입니다.
* **총 반복 횟수 = 바깥쪽 반복 횟수 × 안쪽 반복 횟수**

```c
for(...)       // 바깥쪽 루프
{
    for(...)   // 안쪽 루프
    {
        실행문;
    }
}
```

### 2) 실습 예제 (`for5.c`)
```c
// for5.c
#include <stdio.h>

int main()
{
    int i, j;
    
    // 바깥쪽: 1 ~ 3 (3회)
    for(i = 1; i <= 3; i++) { 
        // 안쪽: 1 ~ 5 (5회)
        for(j = 1; j <= 5; j++) { 
            printf("i=%d, j=%d  ", i, j);
        }
        putchar('\n'); // 안쪽 루프가 끝나면 줄바꿈
    }

    return 0;
}
```
**실행 결과:**
```bash
i=1,j=1  i=1,j=2  i=1,j=3  i=1,j=4  i=1,j=5
i=2,j=1  i=2,j=2  i=2,j=3  i=2,j=4  i=2,j=5
i=3,j=1  i=3,j=2  i=3,j=3  i=3,j=4  i=3,j=5
```

---

## 6. [LAB] 가로 구구단 출력 (`for6.c`)

### 1) 문제 조건
* **파일명:** `for6.c`
* **제어문:** 이중(중첩) for문을 이용한다.
* **출력:** 2단부터 9단까지 단별로 한 줄씩 가로로 출력한다.
* **예시:** `2x1=2  2x2=4 ... 2x9=18`

### 2) 소스 코드
```c
// for6.c
#include <stdio.h>

int main()
{
    int i, j;

    // 바깥쪽 Loop: 단 (2 ~ 9)
    for(i = 2; i <= 9; i++) { 
        // 안쪽 Loop: 곱해지는 수 (1 ~ 9)
        for(j = 1; j <= 9; j++) { 
            // %-2d: 2칸을 확보하고 왼쪽 정렬
            printf("%dx%d=%-2d  ", i, j, i*j);
        }
        putchar('\n');  // 한 단 출력이 끝나면 줄바꿈
    }

    return 0;
}
```

### 3) 실행 결과
```bash
$ ./for6
2x1=2   2x2=4   2x3=6   ...   2x9=18
3x1=3   3x2=6   3x3=9   ...   3x9=27
...
9x1=9   9x2=18  9x3=27  ...   9x9=81
```

---

## 7. [LAB] 세로 구구단 출력 (`for7.c`)

### 1) 문제 조건
* **파일명:** `for7.c`
* **제어문:** 이중(중첩) for문을 이용한다.
* **출력:** 2단부터 9단이 세로 방향으로 나열되도록 출력한다.
* **핵심:** 행(Line)이 바뀔 때마다 곱해지는 수(`1~9`)가 증가하고, 한 줄 안에서는 단(`2~9`)이 증가해야 합니다. (Loop 순서 교체)

### 2) 소스 코드
```c
// for7.c
#include <stdio.h>

int main()
{
    int i, j;

    // 바깥쪽 Loop: 곱해지는 수 (1 ~ 9) -> 줄(Line)을 결정
    for(j = 1; j <= 9; j++) { 
        // 안쪽 Loop: 단 (2 ~ 9) -> 가로로 출력될 내용 결정
        for(i = 2; i <= 9; i++) { 
            printf("%dx%d=%-2d  ", i, j, i*j);
        }
        putchar('\n'); // 2단~9단의 같은 곱셈 수 출력이 끝나면 줄바꿈
    }

    return 0;
}
```

### 3) 실행 결과
```bash
$ ./for7
2x1=2   3x1=3   4x1=4   ...   9x1=9
2x2=4   3x2=6   4x2=8   ...   9x2=18
...
2x9=18  3x9=27  4x9=36  ...   9x9=81
```

---

## 8. [LAB] 별 찍기 - 직각 삼각형 (`for8.c`)

### 1) 문제 조건
* **파일명:** `for8.c`
* **제어문:** 이중(중첩) for문을 이용한다.
* **출력 결과:**
```text
*
**
***
****
*****
```

### 2) 잘못된 예시 (사각형 출력)
안쪽 반복문이 항상 5번 실행되면 사각형 모양이 됩니다.
```c
// [잘못된 코드]
for(i = 1; i <= 5; i++) { 
    for(j = 1; j <= 5; j++) { // 항상 5번 반복
        printf("*");
    }
    putchar('\n');
}
// 결과: 5x5 별 사각형 출력됨
```

### 3) 정답 소스 코드
안쪽 반복문의 종료 조건을 `j <= i`로 설정하여, 줄이 늘어날 때마다 별의 개수도 늘어나게 합니다.

```c
// for8.c
#include <stdio.h>

int main()
{
    int i, j;

    // 바깥쪽 Loop: 줄(Row) 수 (1 ~ 5)
    for(i = 1; i <= 5; i++) { 
        
        // 안쪽 Loop: 별의 개수
        // 핵심: j는 i만큼만 반복해야 함 (1번째 줄은 1개, 2번째 줄은 2개...)
        for(j = 1; j <= i; j++) { 
            printf("*");
        }
        
        putchar('\n'); // 별을 다 찍고 줄바꿈
    }

    return 0;
}
```

### 4) 실행 결과
```bash
$ ./for8
*
**
***
****
*****
```
