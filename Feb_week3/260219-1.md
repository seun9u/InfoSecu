# 🔄 C언어 for문 완벽 정리 (기초부터 이중 for문까지)

## 1. for문의 기초

### 1) 형식 및 동작 순서
`for`문은 초기식, 조건식, 증감식을 한 줄에 모아놓은 반복문입니다.

```c
int i;

// 순서: ①초깃값 -> ②조건식 -> ③실행문 -> ④증감식 -> ②조건식 ...
for(i = 1; i <= 5; i++)
{
    printf("%d\n", i);  // 실행문
}
```

### 2) 기본 예제 (`for1.c`)
변수 선언 위치에 따른 유효 범위(Scope)를 확인합니다.

```c
// for1.c
#include <stdio.h>

int main()
{
    // C99 표준부터 for문 초기식 내에서 변수 선언 가능
    // 단, 여기서 선언된 i는 for문 블록 {} 안에서만 유효함
    for(int i = 1; i <= 5; i++)
    {
        printf("%d\n", i);
    }

    // printf("%d\n", i);  // [에러] i는 for문이 끝나서 소멸됨
    return 0;
}
```

---

## 2. 변수의 유효 범위 (Scope) 증명

중괄호 `{ }` 블록 내에서 선언된 변수는 해당 블록이 끝나면 사라집니다.

```c
// scope_test.c
#include <stdio.h>

int main() {
    int c = 3;  // main 함수 전체에서 유효

    {
        int a = 1; // 이 블록 안에서만 유효
        printf("a = %d\n", a);

        {
            int b = 2; // 가장 안쪽 블록에서만 유효
            // 상위 블록의 a, c 접근 가능
            printf("a = %d, b = %d, c = %d\n", a, b, c);
        }
        // printf("b = %d\n", b); // [에러] b는 소멸됨
        printf("c = %d\n", c);    // c는 접근 가능
    }
    // printf("a = %d\n", a); // [에러] a는 소멸됨
    
    return 0;
}
```

---

## 3. 구구단 출력하기

### 1) 2단 출력 (`for2.c`)
```c
// for2.c
#include <stdio.h>

int main() {
    int dan = 2;
    int i;

    // 1부터 9까지 반복
    for(i = 1; i <= 9; i++)
        printf("%dx%d=%d  ", dan, i, dan*i);

    putchar('\n');  // 줄바꿈
    return 0;
}
```

### 2) 입력받은 단 출력 (`for3.c`)
```c
// for3.c
#include <stdio.h>

int main() {
    int dan, i;

    printf("구구단 몇 단을 출력하겠습니까? (2 ~ 9) ");
    scanf("%d", &dan);

    for(i = 1; i <= 9; i++)
        printf("%dx%d=%d  ", dan, i, dan*i);

    putchar('\n');
    return 0;
}
```

---

## 4. for문의 무한 루프 (`for4.c`)
조건식을 비워두면(`;;`) 무한 루프가 됩니다. 내부에서 `break`로 탈출해야 합니다.

```c
// for4.c
#include <stdio.h>

int main() {
    int i = 1;

    for(;;) { // 무한 루프
        printf("%d\n", i);
        
        i++; // 증감
        
        // 탈출 조건
        if(i == 6) break;
    }
    return 0;
}
```

---

## 5. 이중 for문 (Nested Loop)

### 1) 구조
반복문 안에 또 다른 반복문이 들어있는 형태입니다.
* **총 반복 횟수 = 바깥쪽 반복 횟수 × 안쪽 반복 횟수**

```c
for(...)       // 바깥쪽 루프
{
    for(...)   // 안쪽 루프
    {
        실행문;
    }
}
```

### 2) 실습 예제 (`for5.c`)
```c
// for5.c
#include <stdio.h>

int main()
{
    int i, j;
    
    // 바깥쪽: 1 ~ 3 (3회)
    for(i = 1; i <= 3; i++) { 
        
        // 안쪽: 1 ~ 5 (5회)
        for(j = 1; j <= 5; j++) { 
            printf("i=%d, j=%d  ", i, j);
        }
        
        putchar('\n'); // 안쪽 루프가 끝나면 줄바꿈
    }

    return 0;
}
```

#### 실행 결과 (3 x 5 = 총 15회 실행)
```bash
$gcc -o for5 for5.c$ ./for5
i=1, j=1  i=1, j=2  i=1, j=3  i=1, j=4  i=1, j=5  
i=2, j=1  i=2, j=2  i=2, j=3  i=2, j=4  i=2, j=5  
i=3, j=1  i=3, j=2  i=3, j=3  i=3, j=4  i=3, j=5  
```
