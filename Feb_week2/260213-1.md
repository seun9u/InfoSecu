# 🔄 C언어 반복문 (while문)과 실습

## 1. while문 기초

### 1) 개념 및 형식
조건식이 **참(True)**인 동안 `{ }` 안의 실행문을 계속 반복하는 제어 구조입니다.

```c
// 초기식 (반복 변수 초기화)
while (조건식)
{
    실행문;
    // 증감식 (반복 변수 변경)
}
```

### 2) 기본 예제 (`while1.c`)
1부터 5까지 숫자를 출력하는 예제입니다.

```c
// while1.c
#include <stdio.h>

int main()
{
    int i = 1;              // [초기식]

    while(i <= 5)           // [조건식] i가 5 이하일 동안 반복
    {
        printf("%d\n", i);  // [실행문]
        i++;                // [증감식] i를 1씩 증가
    }

    return 0;
}
```

#### 실행 결과
```bash
$gcc -o while1 while1.c$ ./while1
1
2
3
4
5
```

---

## 2. [LAB] 숫자 맞추기 게임 (기회 제한 기능 추가)

### 목표
* **변수 `count`**만큼 사용자에게 기회를 줍니다. (예: 5번)
* 반복문을 사용하여 기회 안에서 숫자를 입력받습니다.
* 정답을 맞추면 `break`를 사용하여 반복문을 즉시 종료합니다.
* 기회를 모두 소진하거나 게임이 끝나면 정답을 공개합니다.

### 소스 코드 (`random_game2.c`)

```c
// random_game2.c
#include <stdio.h>   // printf(), scanf()
#include <stdlib.h>  // srand(), rand()
#include <time.h>    // time()

int main()
{
    int random_number;  // 정답(랜덤값) 저장
    int input_number;   // 사용자 입력값 저장
    int i = 1;          // 현재 시도 횟수
    int count = 5;      // 총 기회 횟수

    // 1. 난수 생성 초기화
    srand(time(NULL));
    random_number = rand() % 10 + 1;  // 1 ~ 10 사이 난수

    printf("%d 번의 기회가 있습니다.\n\n", count);

    // 2. 반복문 시작 (1회부터 count회까지)
    while(i <= count)
    {
        printf("%d. 숫자 입력(1 ~ 10): ", i);
        scanf("%d", &input_number);

        // 정답을 맞춘 경우
        if(random_number == input_number)
        {
            printf("축하합니다! 숫자를 맞췄습니다.\n");
            break; // 반복문 탈출
        }

        // 틀린 경우
        printf("숫자가 틀렸습니다.\n");
        i++; // 시도 횟수 증가
    }

    // 3. 최종 결과 확인 (정답 공개)
    printf("random_number: %d, input_number: %d\n", random_number, input_number);

    return 0;
}
```

### 실행 결과 (실패 시나리오)
```bash
$gcc -o random_game2 random_game2.c$ ./random_game2

5 번의 기회가 있습니다.

1. 숫자 입력(1 ~ 10): 2
숫자가 틀렸습니다.
2. 숫자 입력(1 ~ 10): 3
숫자가 틀렸습니다.
3. 숫자 입력(1 ~ 10): 8
숫자가 틀렸습니다.
4. 숫자 입력(1 ~ 10): 9
숫자가 틀렸습니다.
5. 숫자 입력(1 ~ 10): 8
숫자가 틀렸습니다.
random_number: 1, input_number: 8
```
