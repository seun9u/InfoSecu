# 📌 C언어 포인터 심화, 연산자, 제어문 및 난수 생성

## 1. 포인터를 통한 변수 값 변경 및 GDB 분석

### 1) 포인터 기본 원리
변수의 주소를 통해 값을 변경하는 방식입니다.
```c
int a = 3;
int sum;
int *p;

p = &sum;   // p는 sum의 주소를 가리킴
*p = a + 5; // p가 가리키는 곳(sum)에 8을 저장 -> sum = 8과 동일
```

### 2) 실습: 포인터로 값 변경하기 (`prac-10-01.c`)
```c
#include <stdio.h>

int main()
{
    int a = 10;
    int *p = &a; // p는 a의 주소를 가짐

    *p = 20;            // a의 값이 20으로 변경됨
    printf("%d\n", a);  // 20
    
    *p = 50;            // a의 값이 50으로 변경됨
    printf("%d\n", a);  // 50

    return 0;
}
```

### 🐞 GDB 메모리 분석 (Step-by-Step)
```bash
$gcc -o prac-10-01 prac-10-01.c -g$ gdb -q ./prac-10-01

(gdb) b main
(gdb) r
(gdb) n
6           int a = 10;
(gdb) n
7           int *p = &a;

# 변수 및 주소 확인
(gdb) p a
$1 = 10
(gdb) p &a
$2 = (int *) 0x7fffffffe1ac

# 포인터 변수 p 확인
(gdb) p p
$5 = (int *) 0x7fffffffe1ac  <-- a의 주소가 저장됨
(gdb) p *p
$6 = 10                      <-- p가 가리키는 값 (a의 값)

# 값 변경 (*p = 20) 실행 후
(gdb) n
(gdb) p a
$7 = 20                      <-- a의 값이 20으로 변경됨

# 값 변경 (*p = 50) 실행 후
(gdb) n
(gdb) p a
$10 = 50                     <-- a의 값이 50으로 변경됨
```

### 3) 하나의 포인터로 여러 변수 가리키기 (`prac-10-02.c`)
```c
#include <stdio.h>

int main()
{
    int *ptr;
    int num1 = 5;
    int num2 = 8;

    ptr = &num1;             // ptr이 num1을 가리킴
    printf("%d\n", *ptr);    // 5

    ptr = &num2;             // ptr이 num2를 가리킴
    printf("%d\n", *ptr);    // 8

    return 0;
}
```

---

## 2. 함수의 Call by Reference와 scanf 원리

### 1) 왜 포인터가 필요한가?
* `main` 함수에 있는 변수 `num`을 다른 함수(`myscanf`)에서 직접 수정하려면, **변수의 주소**를 넘겨줘야 합니다.
* 이를 **Call by Reference (참조에 의한 호출)**라고 합니다.

### 2) 사용자 정의 scanf 만들기 (`myscanf.c`)
```c
#include <stdio.h>

void myscanf(const char *a, int *b);

int main()
{
    int num = 1;
    // num의 주소(&num)를 전달
    myscanf("%d", &num); 
    printf("num = %d\n", num);

    return 0;
}

// 주소를 받는 포인터 변수 b
void myscanf(const char *a, int *b)
{
    // b가 가리키는 주소에 입력받은 값을 저장
    scanf(a, b); 
}
```

### 🐞 GDB 함수 호출 스택 분석
```bash
(gdb) b main
(gdb) r
(gdb) s  <-- Step Into (함수 내부로 진입)

myscanf (a=0x56557008 "%d", b=0xffffd328) at myscanf.c:17
# b는 main의 num 주소(0xffffd328)를 가지고 있음

(gdb) p *b
$1 = 1   <-- 현재 값 1

# scanf 실행 후 (예: 2 입력)
(gdb) p *b
$2 = 2   <-- 값이 2로 변경됨 (main의 num도 변경됨)
```

---

## 3. 연산자 (Operators)

### 1) 복합 대입 연산자
```c
a = a + 3;  // a += 3;
b = a + 3;  // (주의) a값은 변하지 않음
// 종류: +=, -=, *=, /=, %=
```

### 2) 관계 연산자 (True=1, False=0)
* `>` (초과), `<` (미만), `>=` (이상), `<=` (이하)
* `==` (같다), `!=` (다르다)

### 3) 증감 연산자
* `++a` (전위): 증가 후 연산
* `a++` (후위): 연산 후 증가
* **단독 사용 시:** `++a;`와 `a++;`는 결과가 같습니다.

### 4) 비트 연산자 (Bitwise Operators)
컴퓨터 내부의 비트 단위 연산을 수행합니다.

| 연산자 | 설명 | 예시 (a=3, b=5) |
| :---: | :--- | :--- |
| `&` | **AND** (둘 다 1일 때 1) | `3 & 5` → `1` |
| `|` | **OR** (하나라도 1일 때 1) | `7 | 10` → `15` |
| `^` | **XOR** (다르면 1, 같으면 0) | `3 ^ 5` → `6` |
| `~` | **NOT** (반전, 1의 보수) | `~3` → `-4` |
| `<<` | **Left Shift** (값 * 2^n) | `3 << 2` → `12` |
| `>>` | **Right Shift** (값 / 2^n) | `12 >> 1` → `6` |

#### 비트 연산 상세 계산
```text
[AND 연산] 3 & 5
  0000 0011 (3)
& 0000 0101 (5)
-----------
  0000 0001 (1)

[OR 연산] 7 | 10
  0000 0111 (7)
| 0000 1010 (10)
-----------
  0000 1111 (15)

[Left Shift] 3 << 2
  0000 0011 (3)
  << 2칸 이동
-----------
  0000 1100 (12)
```

---

## 4. 제어문 (If ~ Else)

### 1) 기본 구조
* **C언어:** `if(조건) { 실행문 }` 형식을 엄격히 따릅니다.
* **Python:** `if 조건:` 과 들여쓰기를 사용합니다.

```c
// 단순 if문
if (a == 1) {
    printf("true\n");
}

// if ~ else 문
if (average >= 90) {
    printf("학점: A\n");
} else {
    printf("학점: B 이하\n");
}
```

### 2) [LAB] 성적 처리 프로그램 (`sungjuk.c`)
```c
#include <stdio.h>

int main()
{
    int kor, eng, math, total;
    float average;

    printf("국어 점수: "); scanf("%d", &kor);
    printf("영어 점수: "); scanf("%d", &eng);
    printf("수학 점수: "); scanf("%d", &math);

    total = kor + eng + math;
    average = total / 3.0; // 실수 나눗셈을 위해 3.0 사용 권장

    printf("\n>>> 점수 결과 <<<\n");
    printf("총점 : %d\n", total);
    printf("평균 : %.2f\n", average);

    if(average >= 90)
        printf("학점: A\n");

    return 0;
}
```

---

## 5. [LAB] 난수 생성과 숫자 맞추기 게임

### 1) 난수 생성의 원리 (`rand`, `srand`, `time`)
* **`rand()`:** 난수를 생성하지만, 시드(Seed) 값이 고정되어 있으면 매번 같은 패턴이 나옵니다.
* **`srand(time(NULL))`:** 현재 시간을 시드 값으로 사용하여 매번 다른 난수를 생성하게 합니다.

### 2) 숫자 맞추기 게임 (`random_game.c`)
```c
#include <stdio.h>   // printf, scanf
#include <stdlib.h>  // srand, rand
#include <time.h>    // time

int main()
{
    int random_number;
    int input_number;

    // 1. 난수 초기화 (현재 시간 기준)
    srand(time(NULL));

    // 2. 1 ~ 10 사이의 난수 생성
    // rand() % 10 : 0 ~ 9
    // + 1         : 1 ~ 10
    random_number = rand() % 10 + 1;

    // 3. 사용자 입력
    printf("숫자 입력(1 ~ 10): ");
    scanf("%d", &input_number);

    // 4. 결과 비교
    if (random_number == input_number) {
        printf("축하합니다! 숫자를 맞췄습니다.\n");
    } else {
        printf("숫자가 틀렸습니다.\n");
    }

    printf("정답: %d, 입력값: %d\n", random_number, input_number);

    return 0;
}
```
