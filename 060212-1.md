# 🔄 다양한 자료형 값 전달하기 (Passing Data Types)

## 1. 예제 개요
함수의 매개변수(Parameter)는 정수(`int`)뿐만 아니라 문자(`char`), 실수(`float`, `double`), 포인터 등 다양한 자료형을 받을 수 있습니다.

## 2. 소스 코드 (`ex_func_datatype.c`)

```c
// ex_func_datatype.c
#include <stdio.h>

/* [함수 목록]
   함수 1 : void func_char(char c)
   함수 2 : void func_int(int i)
   함수 3 : void func_float(float f)    (작성 예정)
   함수 4 : void func_double(double d)  (작성 예정)
   함수 5 : void func_char2(char *c)    (작성 예정)
*/

// 함수 선언 (Prototype)
void func_char(char c);
void func_int(int i);

int main() {
    char c = 'A';
    int i = 2026;

    func_char(c); // 'A' 전달
    func_int(i);  // 2026 전달

    return 0;
}

// 함수 정의: char형 변수를 받아서 처리
void func_char(char c) {
    // 넘어온 값을 1 증가해서 출력한다. ('A' + 1 = 'B')
    printf("c = %c\n", c + 1);
}

// 함수 정의: int형 변수를 받아서 처리
void func_int(int i) {
    // 넘어온 값을 1 증가해서 출력한다. (2026 + 1 = 2027)
    printf("i = %d\n", i + 1);
}
```

## 3. 실행 결과 분석
1. **`func_char('A')` 호출:**
    * 문자 `'A'`는 아스키 코드 값 **65**입니다.
    * `c + 1`은 66이 되며, 이는 문자 **'B'**에 해당합니다.
    * **출력:** `c = B`

2. **`func_int(2026) 호출`:**
    * 정수 **2026**을 전달받습니다.
    * `i + 1`은 **2027**이 됩니다.
    * **출력:** `i = 2027`
